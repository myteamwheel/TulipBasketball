"use client";

import { useMemo, useState } from "react";
import type { TradeCalculatorAsset } from "@/lib/tradeFinder";
import { calculateTradeBalance } from "@/lib/tradeValue";

const points = (value: number) => Math.round(value).toLocaleString("en-US");
function verdictLabel(verdict: ReturnType<typeof calculateTradeBalance>["verdict"], primaryName: string, otherName: string) {
  if (verdict === "EVEN") return "Near even after package adjustment";
  if (verdict === "LEAN_GET") return `Slight lean ${primaryName}`;
  if (verdict === "FAVORS_GET") return `Favors ${primaryName}`;
  if (verdict === "HEAVILY_FAVORS_GET") return `Strongly favors ${primaryName}`;
  if (verdict === "LEAN_GIVE") return `Slight lean ${otherName}`;
  if (verdict === "FAVORS_GIVE") return `Favors ${otherName}`;
  return `Strongly favors ${otherName}`;
}
function Side({ title, assets, ids, add, remove }: { title: string; assets: TradeCalculatorAsset[]; ids: string[]; add: (id: string) => void; remove: (id: string) => void }) {
  const selected = ids.map((id) => assets.find((asset) => asset.id === id)).filter(Boolean) as TradeCalculatorAsset[];
  return <div className="rounded-lg border border-neutral-800 bg-neutral-950/60 p-3"><div className="mb-2 text-[10px] font-semibold uppercase tracking-wide text-neutral-500">{title}</div><select defaultValue="" onChange={(event) => { if (event.target.value) add(event.target.value); event.target.value = ""; }} className="h-9 w-full rounded-md border border-neutral-700 bg-neutral-900 px-2 text-xs text-neutral-200"><option value="">Add player or pick…</option>{assets.filter((asset) => !ids.includes(asset.id)).map((asset) => <option key={asset.id} value={asset.id}>{asset.position} · {asset.name} · {points(asset.value)}</option>)}</select><div className="mt-2 space-y-1.5">{selected.map((asset) => <div key={asset.id} className="flex items-center justify-between gap-2 rounded-md border border-neutral-800 bg-neutral-900 px-2.5 py-2"><div className="min-w-0"><div className="truncate text-xs font-medium text-neutral-100">{asset.name}</div><div className="text-[9px] text-neutral-600">{asset.position} · KTC {points(asset.value)}</div></div><button onClick={() => remove(asset.id)} className="text-[10px] text-neutral-600 hover:text-red-300">Remove</button></div>)}</div></div>;
}

export default function TradeCalculator({ assets, managers, primaryManagerId, primaryManagerName, ktcStale, pickMarketAvailable }: { assets: TradeCalculatorAsset[]; managers: { id: string; name: string }[]; primaryManagerId: string; primaryManagerName: string; ktcStale: boolean; pickMarketAvailable: boolean }) {
  const others = managers.filter((manager) => manager.id !== primaryManagerId);
  const [otherManagerId, setOtherManagerId] = useState(others[0]?.id ?? "");
  const [giveIds, setGiveIds] = useState<string[]>([]); const [getIds, setGetIds] = useState<string[]>([]);
  const primaryAssets = useMemo(() => assets.filter((asset) => asset.managerId === primaryManagerId), [assets, primaryManagerId]);
  const otherAssets = useMemo(() => assets.filter((asset) => asset.managerId === otherManagerId), [assets, otherManagerId]);
  const otherName = managers.find((manager) => manager.id === otherManagerId)?.name ?? "Other team";
  const give = giveIds.map((id) => primaryAssets.find((asset) => asset.id === id)).filter(Boolean) as TradeCalculatorAsset[];
  const get = getIds.map((id) => otherAssets.find((asset) => asset.id === id)).filter(Boolean) as TradeCalculatorAsset[];
  const balance = calculateTradeBalance(give, get);
  const staleSelection = [...give, ...get].some((asset) => asset.isStale);
  const canEvaluate = give.length > 0 && get.length > 0 && !ktcStale && !staleSelection;
  return <div className="space-y-4"><section className="rounded-lg border border-neutral-800 bg-neutral-900 p-3 sm:p-4"><h2 className="text-sm font-semibold text-neutral-100">Manual Trade Calculator</h2><p className="mt-1 max-w-3xl text-[11px] leading-4 text-neutral-500">Fresh KTC-anchored values with a package adjustment so secondary pieces are not treated as perfectly additive against one premium asset.</p>{ktcStale ? <div className="mt-3 rounded-md border border-amber-900 bg-amber-950/20 px-3 py-2 text-[10px] text-amber-300">Current KTC is unavailable. Trade verdicts are paused.</div> : null}{!pickMarketAvailable ? <div className="mt-2 rounded-md border border-amber-900 bg-amber-950/20 px-3 py-2 text-[10px] text-amber-300">Picks are hidden because current pick values could not be verified.</div> : null}<label className="mt-4 block max-w-sm text-[9px] uppercase tracking-wide text-neutral-600">Other team<select value={otherManagerId} onChange={(event) => { setOtherManagerId(event.target.value); setGetIds([]); }} className="mt-1 h-9 w-full rounded-md border border-neutral-700 bg-neutral-950 px-2 text-xs text-neutral-200">{others.map((manager) => <option key={manager.id} value={manager.id}>{manager.name}</option>)}</select></label></section><div className="grid gap-3 lg:grid-cols-2"><Side title={`${primaryManagerName} gives`} assets={primaryAssets} ids={giveIds} add={(id) => setGiveIds((value) => [...value,id])} remove={(id) => setGiveIds((value) => value.filter((x) => x !== id))}/><Side title={`${otherName} gives`} assets={otherAssets} ids={getIds} add={(id) => setGetIds((value) => [...value,id])} remove={(id) => setGetIds((value) => value.filter((x) => x !== id))}/></div><section className="rounded-lg border border-neutral-800 bg-neutral-900 p-3 sm:p-4">{!give.length || !get.length ? <div className="py-6 text-center text-xs text-neutral-600">Add at least one asset to both sides.</div> : !canEvaluate ? <div className="py-6 text-center text-xs text-amber-300">A current verdict is unavailable until all selected assets and the KTC anchor are fresh.</div> : <><div className="grid grid-cols-2 gap-2 sm:grid-cols-4">{[["Orlando raw",balance.give.rawValue],["Orlando adjusted",balance.give.adjustedValue],[`${otherName} raw`,balance.get.rawValue],[`${otherName} adjusted`,balance.get.adjustedValue]].map(([label,value]) => <div key={String(label)} className="rounded-md bg-neutral-950 p-2.5"><div className="text-[9px] uppercase tracking-wide text-neutral-600">{label}</div><div className="mt-1 text-base font-semibold tabular-nums text-neutral-100">{points(Number(value))}</div></div>)}</div><div className="mt-3 rounded-md border border-neutral-800 bg-neutral-950 p-3"><div className="text-[9px] uppercase tracking-wide text-neutral-600">Adjusted verdict</div><div className="mt-1 text-sm font-semibold text-neutral-100">{verdictLabel(balance.verdict, primaryManagerName, otherName)}</div><div className="mt-1 text-[10px] text-neutral-600">Value balance {balance.fairnessPercent.toFixed(1)}% · Orlando adjusted edge {balance.adjustedEdge > 0 ? "+" : ""}{points(balance.adjustedEdge)}</div></div><p className="mt-2 text-[10px] leading-4 text-neutral-600">Value balance is closeness between adjusted package totals. It is not an acceptance probability.</p></>}</section></div>;
}
