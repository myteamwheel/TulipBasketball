import Link from "next/link";
import { Prisma } from "@/generated/prisma/client";
import { prisma } from "@/lib/prisma";
import { getObservationSeries, closestObservation, computeMarketDataForPlayers } from "@/lib/metrics";
import { fetchFreshDraftPickMarketValues } from "@/lib/pickMarket";
import { calculatePackageTradeValue } from "@/lib/tradeValue";
import { SLEEPER_LEAGUE_ID } from "@/lib/config";
import { publicTeamName } from "@/lib/publicIdentity";
import { formatDateTimeEastern, formatPoints, formatSigned, trendColorClass } from "@/lib/format";

const TYPE_LABEL:Record<string,string>={trade:"Trade",waiver:"Waiver claim",free_agent:"Free agent",commissioner:"Commissioner move"};const PAGE_SIZE=50;
type TradedPick={season:string;round:number;roster_id:number;previous_owner_id:number;owner_id:number};type TxFilter="all"|"trade"|"moves";
function parseRecord(value:string|null):Record<string,number>{if(!value)return{};try{const parsed=JSON.parse(value);return parsed&&typeof parsed==="object"&&!Array.isArray(parsed)?parsed as Record<string,number>:{};}catch{return{}}}function parseArray<T>(value:string|null):T[]{if(!value)return[];try{const parsed=JSON.parse(value);return Array.isArray(parsed)?parsed as T[]:[];}catch{return[]}}function moveOutcome(net:number|null){if(net===null)return null;if(net>=1200)return"Large value gain";if(net>=400)return"Positive value gain";if(net>=-300)return"Roughly neutral";if(net>=-1000)return"Negative value move";return"Large value loss";}function pageHref(filter:TxFilter,page:number){const params=new URLSearchParams();if(filter!=="all")params.set("type",filter);if(page>1)params.set("page",String(page));return params.toString()?`/transactions?${params}`:"/transactions";}export const dynamic="force-dynamic";

export default async function TransactionsPage({searchParams}:{searchParams:Promise<{type?:string;page?:string}>}){
 const params=await searchParams;const filter:TxFilter=params.type==="trade"?"trade":params.type==="moves"?"moves":"all";const requestedPage=Math.max(1,Number.parseInt(params.page??"1",10)||1);const txWhere:Prisma.TransactionWhereInput={league:{sleeperId:SLEEPER_LEAGUE_ID},...(filter==="trade"?{type:"trade"}:filter==="moves"?{type:{not:"trade"}}:{})};const totalMatching=await prisma.transaction.count({where:txWhere});const totalPages=Math.max(1,Math.ceil(totalMatching/PAGE_SIZE));const page=Math.min(requestedPage,totalPages);
 const [transactions,managers,leaguePlayerRefs,pickMarket]=await Promise.all([prisma.transaction.findMany({where:txWhere,orderBy:{sleeperCreatedAt:"desc"},skip:(page-1)*PAGE_SIZE,take:PAGE_SIZE}),prisma.manager.findMany({where:{league:{sleeperId:SLEEPER_LEAGUE_ID}}}),prisma.ownershipInterval.findMany({where:{manager:{league:{sleeperId:SLEEPER_LEAGUE_ID}}},select:{playerId:true}}),fetchFreshDraftPickMarketValues().catch(()=>[])]);const leaguePlayerIds=[...new Set(leaguePlayerRefs.map((row)=>row.playerId))];const players=await prisma.player.findMany({where:{id:{in:leaguePlayerIds}},select:{id:true,sleeperId:true,fullName:true,position:true}});const [series,currentMarket]=await Promise.all([getObservationSeries(leaguePlayerIds),computeMarketDataForPlayers(leaguePlayerIds)]);const managerByRosterId=new Map(managers.map((manager)=>[manager.sleeperRosterId,manager]));const playerBySleeperId=new Map(players.map((player)=>[player.sleeperId,player]));const pickMarketAvailable=pickMarket.length>0;const managerName=(rosterId:number)=>{const manager=managerByRosterId.get(rosterId);return manager?publicTeamName(manager):`Team ${rosterId}`;};
 function playerAsset(sleeperPid:string,rosterId:number,txDate:Date,kind:"add"|"drop"){const player=playerBySleeperId.get(sleeperPid);if(!player)return{assetType:"player" as const,label:"Unmapped player",managerName:managerName(rosterId),rosterId,valueAtTx:null,currentValue:null,currentValueFresh:false,playerId:null,kind,valueAtTxApprox:false};const observations=series.get(player.id)??[];const atTx=closestObservation(observations,txDate,"before")??closestObservation(observations,txDate,"after");const market=currentMarket.get(player.id);const currentValueFresh=!!market&&!market.isStale&&market.currentValue!==null;return{assetType:"player" as const,label:`${player.fullName} (${player.position})`,managerName:managerName(rosterId),rosterId,valueAtTx:atTx?.value??null,valueAtTxApprox:!!atTx&&atTx.observedAt.getTime()!==txDate.getTime(),currentValue:currentValueFresh?market!.currentValue:null,currentValueFresh,playerId:player.id,kind};}
 function currentPickValue(season:string,round:number){const matching=pickMarket.filter((pick)=>pick.season===String(season)&&pick.round===Number(round));const generic=matching.find((pick)=>pick.slot===null);if(generic)return generic.value;if(!matching.length)return null;return Math.round(matching.reduce((sum,pick)=>sum+pick.value,0)/matching.length);}
 const rows=transactions.map((transaction)=>{const adds=parseRecord(transaction.adds),drops=parseRecord(transaction.drops),draftPicks=parseArray<TradedPick>(transaction.draftPicks),rosterIds=parseArray<number>(transaction.rosterIdsInvolved);const playerAdds=Object.entries(adds).map(([pid,rosterId])=>playerAsset(pid,rosterId,transaction.sleeperCreatedAt,"add"));const playerDrops=Object.entries(drops).map(([pid,rosterId])=>playerAsset(pid,rosterId,transaction.sleeperCreatedAt,"drop"));const pickAssets=draftPicks.map((pick)=>({assetType:"pick" as const,label:`${pick.season} Round ${pick.round} · ${managerName(pick.roster_id)} original`,managerName:managerName(pick.owner_id),rosterId:pick.owner_id,previousManagerName:managerName(pick.previous_owner_id),valueAtTx:null,valueAtTxApprox:false,currentValue:currentPickValue(pick.season,pick.round),currentValueFresh:pickMarketAvailable,playerId:null,kind:"add" as const}));const assets=[...playerAdds,...playerDrops,...pickAssets];const tradeSides=[...new Set(rosterIds)].map((rosterId)=>{const acquired=[...playerAdds.filter((asset)=>asset.rosterId===rosterId),...pickAssets.filter((asset)=>asset.rosterId===rosterId)];const currentKnown=acquired.filter((asset)=>asset.currentValue!==null&&asset.currentValueFresh);const complete=acquired.length>0&&currentKnown.length===acquired.length;const rawTotal=currentKnown.reduce((sum,asset)=>sum+(asset.currentValue??0),0);const packageValue=complete?calculatePackageTradeValue(currentKnown.map((asset)=>({value:asset.currentValue??0,assetType:asset.assetType,name:asset.label}))):null;return{rosterId,name:managerName(rosterId),rawTotal,adjustedTotal:packageValue?.adjustedValue??null,packageAdjustment:packageValue?.consolidationAdjustment??null,covered:currentKnown.length,totalAssets:acquired.length,complete};}).filter((side)=>side.totalAssets>0);const tradeCoverageComplete=tradeSides.length>=2&&tradeSides.every((side)=>side.complete&&side.adjustedTotal!==null);const sortedSides=tradeCoverageComplete?[...tradeSides].sort((a,b)=>(b.adjustedTotal??0)-(a.adjustedTotal??0)):[];const currentWinner=transaction.type==="trade"&&sortedSides.length>=2?sortedSides[0]:null;const runnerUp=currentWinner?sortedSides[1]:null;const rosterMoveAssets=[...playerAdds,...playerDrops];const moveCoverageComplete=rosterMoveAssets.length>0&&rosterMoveAssets.every((asset)=>asset.valueAtTx!==null);const addAtTx=moveCoverageComplete?playerAdds.reduce((sum,asset)=>sum+(asset.valueAtTx??0),0):0;const dropAtTx=moveCoverageComplete?playerDrops.reduce((sum,asset)=>sum+(asset.valueAtTx??0),0):0;const rosterMoveNet=transaction.type!=="trade"&&moveCoverageComplete?addAtTx-dropAtTx:null;return{...transaction,assets,tradeSides,tradeCoverageComplete,currentWinner,runnerUp,rosterMoveNet,rosterMoveOutcome:moveOutcome(rosterMoveNet)};});
 return <div className="min-w-0 space-y-4"><div><h1 className="text-xl font-semibold text-neutral-100">Transactions</h1><p className="mt-1 max-w-3xl text-sm leading-5 text-neutral-500">Full synced history in pages of {PAGE_SIZE}. Historical player values require an observation within seven days; current trade comparisons require complete fresh coverage.</p></div><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div className="flex flex-wrap gap-1">{[["all","All"],["trade","Trades"],["moves","Adds / waivers"]].map(([key,label])=><Link key={key} href={pageHref(key as TxFilter,1)} className={`rounded-md px-3 py-1.5 text-[11px] ${filter===key?"bg-neutral-700 text-neutral-100":"border border-neutral-800 bg-neutral-900 text-neutral-500"}`}>{label}</Link>)}</div><div className="text-[10px] text-neutral-600">{totalMatching.toLocaleString("en-US")} matching · page {page}/{totalPages}</div></div>{!pickMarketAvailable?<div className="rounded-lg border border-amber-900/70 bg-amber-950/20 p-3 text-[10px] leading-4 text-amber-300">Trades containing draft picks are withheld from current comparison until a fresh pick market is available.</div>:null}<div className="space-y-3">{rows.map((transaction)=><article key={transaction.id} className="min-w-0 rounded-lg border border-neutral-800 bg-neutral-900 p-3 sm:p-4"><div className="mb-3 flex items-center justify-between gap-3 text-[10px] text-neutral-600"><span className="rounded bg-neutral-800 px-2 py-1 font-medium text-neutral-300">{TYPE_LABEL[transaction.type]??transaction.type}</span><span>{formatDateTimeEastern(transaction.sleeperCreatedAt.toISOString())}</span></div>{transaction.type==="trade"&&transaction.tradeSides.length>=2?<div className="mb-3 rounded-lg border border-neutral-800 bg-neutral-950 p-3"><div className="flex flex-wrap items-center justify-between gap-2"><span className="text-[10px] font-medium uppercase tracking-wide text-neutral-500">Current trade value</span>{transaction.tradeCoverageComplete&&transaction.currentWinner&&transaction.runnerUp?<span className="text-[10px] text-emerald-300">Complete fresh coverage · adjusted edge {formatSigned((transaction.currentWinner.adjustedTotal??0)-(transaction.runnerUp.adjustedTotal??0))}</span>:<span className="text-[10px] text-amber-300">Incomplete/stale coverage · no current leader</span>}</div><div className="mt-2 grid gap-2 sm:grid-cols-2">{transaction.tradeSides.map((side)=><div key={side.rosterId} className="grid grid-cols-[1fr_auto] items-center gap-3 rounded-md bg-neutral-900 px-3 py-2"><span className="truncate text-xs text-neutral-300">{side.name}</span><div className="text-right"><div className="text-sm font-semibold text-neutral-100">{side.adjustedTotal!==null?formatPoints(side.adjustedTotal):"—"}</div><div className="text-[9px] text-neutral-600">adjusted · raw {formatPoints(side.rawTotal)}</div><div className={`text-[9px] ${side.complete?"text-neutral-700":"text-amber-500"}`}>{side.covered}/{side.totalAssets} fresh assets valued</div></div></div>)}</div>{transaction.tradeCoverageComplete&&transaction.currentWinner?<p className="mt-2 text-[10px] text-neutral-500">Current adjusted-value leader: <span className="font-medium text-emerald-300">{transaction.currentWinner.name}</span>. Today’s market value only.</p>:null}</div>:null}{transaction.type!=="trade"?(transaction.rosterMoveNet!==null?<div className="mb-3 flex items-center justify-between gap-3 rounded-md border border-neutral-800 bg-neutral-950 px-3 py-2 text-xs"><span className="text-neutral-500">Value change at the move</span><span className={`text-right ${trendColorClass(transaction.rosterMoveNet)}`}><strong>{transaction.rosterMoveOutcome}</strong><br/><span className="font-normal">{formatSigned(transaction.rosterMoveNet)}</span></span></div>:transaction.assets.length?<div className="mb-3 rounded-md border border-neutral-800 bg-neutral-950 px-3 py-2 text-[10px] text-neutral-500">Historical value unavailable within the seven-day tolerance, so no outcome label is shown.</div>:null):null}<div className="grid grid-cols-1 gap-2 sm:grid-cols-2">{transaction.assets.map((asset,index)=><div key={index} className="grid grid-cols-[1fr_auto] items-center gap-3 rounded-md bg-neutral-950 px-3 py-2"><div className="min-w-0"><div className="truncate text-xs"><span className={asset.kind==="add"?"text-emerald-400":"text-red-400"}>{asset.kind==="add"?"+ ":"− "}</span>{asset.playerId?<Link href={`/players/${asset.playerId}`} className="text-neutral-100 hover:text-emerald-300">{asset.label}</Link>:<span className="text-neutral-100">{asset.label}</span>}</div><div className="mt-0.5 truncate text-[9px] text-neutral-600">{asset.managerName}{asset.assetType==="pick"&&"previousManagerName" in asset?` · from ${asset.previousManagerName}`:""}</div></div><div className="text-right text-[10px]">{asset.assetType==="player"?<><div className="text-neutral-500">at move {formatPoints(asset.valueAtTx)}{asset.valueAtTxApprox?<span className="text-neutral-700"> · nearest</span>:null}</div>{asset.currentValue!==null&&asset.valueAtTx!==null?<div className={trendColorClass(asset.currentValue-asset.valueAtTx)}>now {formatPoints(asset.currentValue)} · {formatSigned(asset.currentValue-asset.valueAtTx)}</div>:<div className="text-amber-700">current stale/unavailable</div>}</>:<div className={asset.currentValueFresh?"text-neutral-400":"text-amber-700"}>{asset.currentValueFresh?`current ${formatPoints(asset.currentValue)}`:"current unavailable"}</div>}</div></div>)}{transaction.assets.length===0?<p className="text-xs text-neutral-500">No player or pick assets recorded.</p>:null}</div></article>)}{rows.length===0?<p className="text-sm text-neutral-500">No matching synced transactions.</p>:null}</div>{totalPages>1?<nav className="flex items-center justify-between gap-3 rounded-lg border border-neutral-800 bg-neutral-900 p-3"><Link href={pageHref(filter,Math.max(1,page-1))} className={`rounded-md border px-3 py-1.5 text-xs ${page<=1?"pointer-events-none border-neutral-900 text-neutral-700":"border-neutral-700 text-neutral-300"}`}>← Newer</Link><span className="text-[10px] text-neutral-600">{((page-1)*PAGE_SIZE+1).toLocaleString("en-US")}–{Math.min(page*PAGE_SIZE,totalMatching).toLocaleString("en-US")} of {totalMatching.toLocaleString("en-US")}</span><Link href={pageHref(filter,Math.min(totalPages,page+1))} className={`rounded-md border px-3 py-1.5 text-xs ${page>=totalPages?"pointer-events-none border-neutral-900 text-neutral-700":"border-neutral-700 text-neutral-300"}`}>Older →</Link></nav>:null}</div>;
}
